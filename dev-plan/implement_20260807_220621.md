# implement_20260807_220621.md

작성 일시: `2026-08-07 22:06:21 KST`

최근 업데이트: `2026-08-08 KST` — Phase 7에서 통합 runtime 모듈·private 상태 Route·D1 상태 테이블로 실행 경로를 정리하고, 기준선·전체 자동 검증을 다시 통과했다. 분리 실행 소스 디렉터리의 물리 삭제는 기존 미커밋 변경 보존을 위해 명시 승인 대기다.

이 문서는 AI Systems Atlas의 로컬 우선 핵심 기능을 완성하는 개발 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

- 새로운 `.md`·`.mdx` 또는 GitHub `README.md`·`dev-plan/**/*.md`가 추가되면 검증, 파싱, 노드·관계 저장, 그래프 갱신까지 한 흐름으로 완료한다.
- 규칙 기반 Markdown 그래프는 OAuth·Codex 상태와 무관하게 항상 생성하고 조회할 수 있게 유지한다.
- `@openai/codex-sdk`와 기존 Codex/ChatGPT OAuth 세션을 웹앱과 같은 로컬 Node 실행 단위에서 사용하고 별도 분리 실행을 제거한다.
- 대시보드의 장치 신호 상태를 실제 OAuth·문서 처리·보강 작업 상태로 교체한다.
- 새 문서의 노드가 500노드 기본 투영에 포함되지 않아도 검색, 최근 반영 문서, 문서 중심 그래프로 확인할 수 있게 한다.

## 개발 범위

- 로컬 D1 백업·복구와 데이터 기준선 고정
- 수동 Markdown 추가·중복·재인덱싱·삭제 계약 보강
- 문서 저장 후 graph fingerprint 무효화와 열린 그래프 자동 재조회
- 별도 분리 프로세스 없는 통합 Codex OAuth 작업 실행기
- GitHub Discovery·Preview·Apply 작업의 웹앱 런타임 통합
- 대시보드 상태 모델과 실패·재시도·취소 UX 정리
- 신규 문서 확인 경로와 관계 중심 투영 보강
- 구형 분리 실행 구조 정리와 전체 회귀 검증

## 제외 범위

- Cloudflare 원격 D1 생성, 외부 배포, 운영 DB 이전
- 다중 사용자 SaaS 계정·과금·조직 권한 시스템
- OpenAI API Key, Responses API, `LIGHTRAG_API_KEY` 기반 구현
- LightRAG 또는 별도 Graph-RAG 서버 도입
- 현재 Three.js 그래프 GUI·별자리·성운·궤도 디자인의 전면 교체
- 근거 없는 화면 연결을 사실 관계로 D1에 저장
- 전체 853문서 재파싱을 구현 편의상 무조건 실행

## 참조 문서

- [README](../README.md)
- [현재 OAuth 런타임 방향](../docs/current-oauth-runtime.md)
- [이전 통합 런타임 계획](./implement_20260807_203534.md)
- [Knowledge Graph Ontology v1](../docs/knowledge-graph-ontology-v1.md)
- [Gold Graph 시각 QA](../docs/gold-graph-visual-qa-20260806.md)

## 문서 기반 제약 사항

- `API 미사용`은 OpenAI 유료 API·API Key를 사용하지 않는다는 뜻이며 Atlas 내부 HTTP Route와 D1 접근은 유지한다.
- OAuth 토큰은 브라우저, 일반 D1 테이블, 작업 응답, 로그에 기록하지 않는다.
- Codex 보강 실패가 이미 저장된 규칙 그래프를 롤백하거나 손상시키면 안 된다.
- GitHub Apply는 manifest digest, Blob SHA, stage checksum, 문서 경계 원자 저장 계약을 유지한다.
- 전체 저장 수량과 화면의 최대 500노드·2,000선 투영 수량을 항상 구분한다.

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 번에 하나의 증분만 구현하고 브라우저 결과를 확인한 뒤 다음 증분으로 이동한다.
- 증분 결과가 승인되지 않으면 해당 증분만 되돌릴 수 있도록 커밋 경계를 작게 유지한다.
- 기존 저장·검증·Lease·citation 계약을 우선 재사용하고 새 의존성은 꼭 필요한 경우에만 추가한다.
- 데이터 변경 전 로컬 D1 백업을 만들고 복구 가능성을 확인한다.

## 현재 데이터 기준선

2026-08-08 Phase 2 임시 fixture 제거 후 로컬 D1 읽기 전용 감사 결과다.

| 항목 | 현재 값 |
|---|---:|
| 문서 | 853 |
| 근거 블록 | 148,655 |
| 문서별 노드 mention | 99,393 |
| 고유 엔티티 | 89,669 |
| 규칙 관계 | 94,487 |
| Codex 근거 관계 | 1 |
| 전체 저장 관계 | 94,488 |
| 보강 작업 | 10,904 |
| 보강 대기 | 10,903 |

- [x] `atlas-md-ingestion-smoke-20260807.md` 추가로 8노드·7관계가 생성됨을 확인했다.
- [x] 동일 내용 재처리가 `unchanged`로 끝나는 것을 확인했다.
- [x] Phase 2 완료 후 임시 smoke 문서·전용 노드·관계·작업을 삭제하고 실제 853문서 코퍼스로 복구했다.

## 예상 변경 파일 / 영향 범위

| 영역 | 예상 파일 | 책임 |
|---|---|---|
| 통합 런타임 | `server/runtime/*`, `scripts/start-integrated-app.mjs` | 서버 수명주기, 작업 실행, 종료·복구 |
| Codex OAuth | `server/codex/*`, `app/lib/llm/*`, OAuth 상태 Route | SDK 실행, 상태 판정, 결과 검증 |
| 저장·작업 | `app/lib/storage/*`, `db/schema.ts`, `drizzle/*` | Lease·retry·원자 저장·fingerprint |
| Markdown | `app/lib/ingestion/*`, `app/api/documents/*` | 추가·중복·재인덱싱·삭제 영수증 |
| GitHub | `app/api/github/*`, GitHub source 모듈 | Discovery·Preview·Apply 통합 |
| 대시보드 | `app/dashboard/dashboard-client.tsx` | 실제 인증·작업 상태와 사용자 동작 |
| 그래프 | `app/knowledge-graph.tsx`, graph Route·repository | 자동 갱신·문서 중심 확인 경로 |
| 검증 | `tests/*.test.mjs`, `scripts/*audit*` | 데이터·보안·회귀 감사 |

## Phase 상태 요약

- [x] Phase 1. 데이터 기준선·백업·복구 고정
- [x] Phase 2. Markdown 추가와 그래프 자동 갱신 완성
- [x] Phase 3. 통합 Codex OAuth 작업 런타임
- [x] Phase 4. GitHub 동기화 런타임 통합
- [x] Phase 5. 대시보드 상태와 작업 UX 전환
- [x] Phase 6. 신규 노드 탐색성과 그래프 투영 보강
- [x] Phase 7. 구형 분리 실행 구조 정리·최종 회귀 검증
- [x] Phase 8. Codex 선별 보강 배치 안전장치·소량 실행 검증

## QA 관점

- [x] 0바이트, 잘못된 확장자, 비 UTF-8, 2MB 초과, 20개 초과 파일을 거부한다.
- [x] 동일 SHA·동일 parser version은 `unchanged`, 내용 변경은 동일 문서 원자 교체로 처리한다.
- [x] 저장 중 실패하거나 서버가 종료돼도 이전 정상 문서·노드·관계를 유지한다.
- [x] OAuth 미로그인·만료·철회에서도 규칙 기반 그래프 생성과 조회가 성공한다.
- [x] Codex timeout·취소·잘못된 JSON·근거 없는 관계를 D1에 병합하지 않는다.
- [x] 서버 재시작 후 Lease 작업이 중복 실행되거나 유실되지 않는다.
- [x] 새 문서가 기본 500노드 투영 밖이어도 검색과 문서 중심 화면에서 확인된다.
- [x] 문서 삭제 후 전용 mention·관계·FTS·cache가 남지 않는다.
- [x] 비공개 GitHub 저장소 메타데이터와 OAuth 정보가 공개 응답·로그에 노출되지 않는다.
- [x] 별자리·성운·궤도, Gold Graph, 500×2,000 fixture의 시각·성능 회귀가 없다.
- [x] 보강 전용·선별 job ID 배치는 GitHub 작업을 claim하지 않고 허용된 작업만 처리한다.

## Phase 1. 데이터 기준선·백업·복구 고정

### 목표

- 핵심 런타임 변경 전에 로컬 D1 정본과 복구 절차를 고정한다.

### 구현 태스크

- [x] 현재 SQLite 경로를 단일 resolver로 확인하고 쓰기 중인 DB와 backup DB를 혼동하지 않게 한다.
- [x] 문서·블록·mention·엔티티·관계·작업 수량과 orphan을 읽기 전용으로 감사한다.
- [x] 테스트 Markdown 유지 여부를 결정하고 기준선 수치를 확정한다.
- [x] 변경 전 D1 backup과 checksum 영수증을 생성한다.
- [x] 별도 임시 DB로 복구하는 절차를 실행 가능한 스크립트와 문서로 고정한다.

### 자체 테스트

- [x] backup과 원본의 핵심 테이블 수량·fingerprint가 일치한다.
- [x] 복구 DB에서 corpus·overview·repository snapshot을 읽을 수 있다.
- [x] 기존 Gold/max fixture는 D1 상태와 무관하게 정상 반환된다.

### 이슈 및 수정

- [x] `--db` 지정 경로가 존재하지 않아도 통과하던 문제를 실제 `.sqlite` 파일 검증으로 수정했다.
- [x] 기존 재처리 backup에 데이터 fingerprint·SHA-256·복구 영수증이 없던 문제를 공통 backup 모듈 재사용으로 수정했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-07 KST

- `npm run db:baseline`에서 integrity `ok`, orphan 0, 중복 관계 0, stage row 0을 확인했다.
- data fingerprint `d79a325ef5fb12dcad7592a86c5d48033a621084e903cb35556a0c33ef367bfd`를 고정했다.
- 655,224,832-byte backup과 SHA-256 `b50cc3b7392a024fa02a1a479fe056358cd32ba261adb57de0057d15bec86a76` 영수증을 생성했다.
- 임시 복구 DB의 integrity와 fingerprint가 원본과 일치했다.
- 복구 DB에서 corpus `500N/2000E`, overview `130N/176E`, repository `500N/276E`, Gold `68N/101E`, max `500N/2000E`를 조회했다.
- 신규 기준선 테스트 3/3, 전체 `npm test` 155/155, production build, ESLint, `git diff --check`를 통과했다.

## Phase 2. Markdown 추가와 그래프 자동 갱신 완성

### 목표

- 사용자가 파일을 추가하면 별도 명령과 전체 새로고침 없이 저장 결과와 그래프 갱신을 확인한다.

### 구현 태스크

- [x] `/api/documents`의 성공·unchanged·부분 실패 영수증을 일관된 계약으로 정리한다.
- [x] 문서 저장 후 corpus·overview·repository cache와 fingerprint를 정확히 무효화한다.
- [x] 대시보드에 파일별 노드·관계·중복·오류 결과를 즉시 표시한다.
- [x] 열린 그래프가 fingerprint 변경을 감지해 snapshot을 자동 재조회한다.
- [x] 업로드·재인덱싱·삭제가 동일한 갱신 계약을 사용하게 한다.

### 자체 테스트

- [x] Markdown 1개 추가 시 문서·노드·관계 수량이 예상치만큼 증가한다.
- [x] 같은 파일 재업로드는 수량을 변경하지 않는다.
- [x] 내용 변경 후 이전 전용 관계가 남지 않고 새 그래프로 교체된다.
- [x] 삭제 후 문서·mention·전용 관계·FTS 결과가 제거된다.
- [x] 열린 그래프와 대시보드가 수동 새로고침 없이 갱신된다.

### 이슈 및 수정

- [x] 첫 오류에서 전체 업로드가 중단되던 동작을 파일별 격리와 HTTP `207 Multi-Status` 영수증으로 수정했다.
- [x] 동일 수량의 relation upsert를 놓칠 수 있던 revision에 relation `created_at` 버전을 포함했다.
- [x] 기존 `results`·`ok`·`result` 응답 필드를 유지해 기존 클라이언트와 전체 회귀 테스트 호환성을 보존했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-08 KST

- 문서 API는 신규 `6N/5E`, 동일 `0N/0E`, 변경 `10N/9E`, 삭제 `-10N/-9E` 영수증을 실제 로컬 D1에서 반환했다.
- 유효 Markdown과 잘못된 `.txt` 동시 요청은 정상 문서를 유지하고 파일별 실패를 포함한 HTTP `207`을 반환했다.
- 열린 corpus 그래프는 새 문서 반영 후 수동 새로고침 없이 `89,677N/94,495E`에서 `89,683N/94,500E`로 바뀌고 삭제 후 원래 수량으로 복구됐다.
- 삭제 후 임시 문서의 document·block·mention·relation·FTS·stage row가 모두 0임을 확인했다.
- 대시보드에서 재인덱싱과 삭제 영수증의 파일명·작업·노드/관계 delta를 시각 확인했다.
- 전체 `npm test` 159/159, production build, Connector build, TypeScript, ESLint, `git diff --check`를 통과했다.
- 최종 실제 코퍼스는 853문서·89,669 고유 엔티티·99,393 mention·94,488 저장 관계이며 orphan·중복·stage row는 0이다.

## Phase 3. 통합 Codex OAuth 작업 런타임

### 목표

- 별도 `npm run connector:start` 없이 웹앱 Node 런타임이 Codex 보강 작업을 처리한다.

### 구현 태스크

- [x] 기존 Connector의 claim·lease·timeout·retry·cancel·graceful shutdown 책임을 목록화한다.
- [x] `@openai/codex-sdk` 실행기를 서버 전용 모듈로 이동하고 기존 구조화 출력 검증기를 재사용한다.
- [x] `connected`, `login_required`, `reauth_required`, `running`, `failed` 상태 계약을 구현한다.
- [x] `OPENAI_API_KEY`, `LIGHTRAG_API_KEY`, Connector token을 실행 환경에 요구하지 않게 한다.
- [x] 서버 시작·종료·재시작에서 작업 poller가 한 번만 동작하도록 수명주기를 고정한다.
- [x] OAuth 실패 시 규칙 그래프를 유지하고 보강 작업만 재시도 가능 상태로 전환한다.

### 자체 테스트

- [x] `codex login` OAuth 세션만으로 구조화 보강 fixture 1건을 완료한다.
- [x] 토큰·세션 원문이 브라우저 응답, D1 일반 테이블, 로그에 포함되지 않는다.
- [x] 취소·timeout·잘못된 JSON·근거 누락 결과가 관계 테이블에 저장되지 않는다.
- [x] 서버 재시작 후 Lease 만료 작업이 한 번만 재개된다.

### 이슈 및 수정

- [x] 기존 10,903개 대기 작업이 통합 런타임에서 무제한 실행될 위험을 provider version claim 경계로 차단했다.
- [x] Vite가 사용 중 포트에서 자동 이동하면 런타임 기준 URL과 달라지는 문제를 통합 실행기의 명시적 `--port` 전달로 수정했다.
- [x] Graph RAG 회귀 테스트가 구형 Connector heartbeat를 사용하던 문제를 통합 runtime state 계약으로 갱신했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-08 KST

- `server/codex/codex-engine.ts`로 SDK 실행 경계를 이동하고 구조화 출력·node ID·relation ontology·block evidence 검증기를 그대로 재사용했다.
- `npm run dev`는 웹앱 준비 후 통합 런타임을 자동 시작하며, 0600 PID lock으로 중복 poller와 stale lock을 구분한다.
- 프로세스 내부 통신용 secret은 실행 시 자동 생성하고 API Key·Connector token과 함께 Codex 자식 환경에서 제거한다.
- 신규 provider `codex-sdk-0.146.0+atlas-runtime.1`만 claim해 기존 10,903개 레거시 대기열을 변경하지 않았다.
- 실제 `codex login` OAuth fixture가 `completed`, 관계 1개, 경고 0개로 완료됐다.
- 실제 임시 Markdown E2E는 규칙 `4N/3E` 저장 후 Codex `tests` 관계 1개를 D1에 병합했고, 삭제 후 853문서·89,669엔티티·94,488관계 및 기존 fingerprint로 복구됐다.
- 상태 API `/api/runtime/codex/status`에서 `connected`, `available: true`, `authenticated: true`를 확인했다.
- singleton·provider claim·migration·timeout·취소·잘못된 결과·근거 검증·Lease 재개 테스트를 포함해 전체 165개 테스트 대상으로 확장했다.

## Phase 4. GitHub 동기화 런타임 통합

### 목표

- README와 `dev-plan/**/*.md`를 별도 Connector 없이 Discovery·Preview·Apply할 수 있게 한다.

### 구현 태스크

- [x] GitHub 인증 상태를 서버 전용 경계로 이동하고 최소 저장소 읽기 권한을 적용한다.
- [x] Discovery·Preview·Apply worker를 웹앱 작업 런타임에 연결한다.
- [x] 기존 manifest digest·Blob SHA·stage chunk·repository atomic apply 계약을 재사용한다.
- [x] 변경 없음·추가·수정·삭제를 저장소별 receipt로 표시한다.
- [x] 실패한 저장소만 격리하고 다른 저장소와 마지막 정상 그래프를 유지한다.

### 자체 테스트

- [x] README 1개 추가, dev-plan 1개 수정, 문서 1개 삭제를 Preview와 Apply가 정확히 구분한다.
- [x] 승인되지 않은 Preview는 Apply되지 않는다.
- [x] 권한 상실·rate limit·네트워크 오류가 저장소별 실패로 끝난다.
- [x] 비공개 저장소 이름·경로·원문 링크가 비인증 응답에 포함되지 않는다.

### 이슈 및 수정

- [x] 기존 source 작업과 신규 통합 작업의 자동 실행 경계가 없던 문제를 runtime generation claim으로 차단했다.
- [x] Codex OAuth preflight 실패가 GitHub 동기화까지 막던 구조를 두 인증의 독립 상태로 분리했다.
- [x] GitHub 자식 프로세스에 API Key·IPC secret 환경이 전달될 수 있던 경계를 keyring 전용 환경으로 축소했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-08 KST

- GitHub 실행기를 `server/github` 서버 전용 모듈로 이동하고 모든 `gh api` 요청을 명시적 GET으로 고정했다.
- `npm run dev`의 singleton runtime이 Codex와 GitHub 작업을 함께 poll하되 GitHub를 먼저 처리하고 인증 실패를 서로 격리한다.
- 신규 작업은 `atlas-integrated-github-runtime-1`로 저장·claim하며 D1 `runtime_version` 열과 claim 인덱스를 추가했다.
- 실제 `gh` keyring OAuth로 `memory_node_graph`의 11개 대상 Markdown·394,613 bytes를 Preview하고 manifest/Blob SHA 재검증을 통과했다.
- 웹 API E2E Preview가 queued → leased → running → completed로 완료됐고 `/api/runtime/github/status`는 `connected`를 반환했다.
- 전체 171개 테스트, production build, Connector/runtime build, TypeScript, ESLint, `git diff --check`를 통과했다.
- 검증 후 D1은 853문서·89,669엔티티·94,488관계, orphan·중복·stage 0, fingerprint `fb0456967b27077f6bb52a30d941c5dd97883d014d0748d4fdb2d3629ed9675d`를 유지했다.

## Phase 5. 대시보드 상태와 작업 UX 전환

### 목표

- 장치 신호가 아니라 사용자가 이해할 수 있는 인증·문서·작업 상태를 표시한다.

### 구현 태스크

- [x] `NO SIGNAL`, ONLINE/OFFLINE, heartbeat 시간, Connector 복구 문구를 제거한다.
- [x] Codex OAuth와 GitHub 인증 상태를 독립적으로 표시한다.
- [x] 문서 분석 중, 기본 그래프 완료, 관계 보강 중, 완료, 재로그인 필요, 실패 상태를 적용한다.
- [x] 재시도·취소 버튼을 실제 통합 런타임 작업 상태와 연결한다.
- [x] 전체 저장 수량과 현재 화면 투영 수량을 함께 설명한다.

### 자체 테스트

- [x] 인증 상태 변경과 작업 진행률이 새로고침 없이 갱신된다.
- [x] OAuth가 없어도 Markdown 추가 버튼과 기본 인덱싱이 정상 동작한다.
- [x] 실패 원인과 다음 사용자 동작이 모순 없이 표시된다.
- [x] 작은 화면에서 하단 제어와 대시보드 작업 버튼이 잘리지 않는다.

### 이슈 및 수정

- [x] fresh heartbeat가 `login_required`·`reauth_required`를 연결 상태로 덮던 판정을 runtime state 우선으로 수정했다.
- [x] 기존 10,903개 대기열이 현재 통합 작업처럼 보이던 집계를 provider version 기준으로 분리했다.
- [x] 문서별 node mention 합계와 고유 저장 노드를 같은 `노드` 수량으로 표시하던 모호함을 저장/투영 인벤토리로 분리했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-08 KST

- 대시보드는 `/api/runtime/codex/status`와 `/api/runtime/github/status`를 5초마다 독립 조회하며 `OAuth 연결됨`, `로그인 필요`, `재로그인 필요`, `관계 보강 중`, `권한 확인`, `요청 제한`, `실행 오류`를 구분한다.
- 상단 인벤토리는 실제 D1의 853문서·89,669 고유 노드·94,488 저장 관계와 화면 보기별 최대 500노드·2,000선을 함께 설명한다.
- 현재 통합 provider 대기열은 0개로 표시하고 기존 10,903개 대기열은 별도 과거 작업으로 안내해 자동 실행 대상으로 오인하지 않게 했다.
- Markdown 추가·기본 파싱·그래프 반영은 OAuth 카드와 독립적으로 유지했고 문서별 재시도·취소는 기존 실제 작업 Route에 연결했다.
- 인앱 브라우저에서 Codex·GitHub 두 OAuth가 각각 연결됨으로 갱신되는 상태, 데스크톱 GUI, 390px 모바일의 가로 넘침 0과 버튼 비잘림을 확인했다.
- 전체 `npm test` 173/173, production build, runtime build, ESLint, `git diff --check`를 통과했다.

## Phase 6. 신규 노드 탐색성과 그래프 투영 보강

### 목표

- 새로 추가한 문서와 노드를 사용자가 그래프에서 확실히 찾고 관계를 검증할 수 있게 한다.

### 구현 태스크

- [x] 문서 ID 또는 최근 반영 문서를 기준으로 한 문서 중심 graph scope를 추가한다.
- [x] 검색 결과에서 대상 노드로 이동하고 1-hop·2-hop 궤도를 열 수 있게 한다.
- [x] 새 노드가 corpus 500노드 예산 밖일 때 그 이유와 확인 경로를 표시한다.
- [x] 구조·명시·Codex 추론·비저장 화면 연결의 범례와 수량을 유지한다.
- [x] cache fingerprint가 바뀌어도 선택 노드와 현재 보기 상태를 가능한 범위에서 보존한다.

### 자체 테스트

- [x] 새 테스트 Markdown의 노드가 문서 중심 그래프와 검색에서 모두 확인된다.
- [x] 선택 노드의 실제 저장 관계와 화면 보조 연결이 구분된다.
- [x] 500×2,000 fixture에서 프레임과 메모리 기준을 유지한다.
- [x] 별자리·성운·궤도 전환과 발광 설정 저장 동작에 회귀가 없다.

### 이슈 및 수정

- [x] D1의 쿼리당 100개 변수 상한을 초과하던 양방향 관계 조회를 40개 ID chunk로 수정했다.
- [x] 현재 500노드의 부분 문자열 검색이 전체 코퍼스 검색 결과를 가리던 우선순위를 제거했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

### 구현·검증 기록 — 2026-08-08 KST

- `scope=document&documentId=...`는 해당 Markdown의 직접 mention을 seed로 삼아 저장된 사실 관계만 1·2단계 확장하고 최대 500노드·2,000관계로 제한한다.
- 상단 검색은 현재 화면 500노드가 아니라 D1 FTS/lexical index 전체를 조회하며, 화면 밖 결과는 출처 Markdown과 함께 표시하고 클릭 즉시 문서 중심 `orbit` 및 선택 노드 URL로 이동한다.
- 하단 데이터 메뉴에 최근 Markdown 6개와 문서별 저장 노드·관계 수를 추가했고, 전체 corpus 제목에는 500노드 예산 밖 노드의 확인 경로를 안내한다.
- 실제 D1에서 `auraltune` 검색 → 화면 밖 README 노드 → 문서 중심 500노드·616 저장 관계 → 선택 노드 CORE/1-hop/2-hop 궤도 이동을 브라우저로 확인했다. 이 문서 scope의 비저장 화면 연결은 0개다.
- 새 테스트 Markdown `phase-6-search-probe.md`의 업로드 → 최근 문서 → 전체 검색 → 문서 중심 graph → 삭제 E2E를 포함해 전체 `npm test` 176/176, production build, runtime build, ESLint를 통과했고 500×2,000 layout benchmark는 기존 성능 기준을 유지했다.

## Phase 7. 구형 분리 실행 구조 정리·최종 회귀 검증

### 목표

- 통합 런타임이 검증된 뒤에만 구형 분리 실행 구조와 혼동되는 코드·문구·환경 변수를 제거한다.

### 구현 태스크

- [x] 재사용 client·runner·run policy를 `server/runtime/`으로 이동하고 TypeScript 빌드 대상을 `.runtime-dist/`로 전환한다.
- [x] 공개 heartbeat·capability Route를 private `/api/runtime/status`로 통합하고 상태 저장소를 `runtime_status`, `github_runtime_status`로 전환한다.
- [x] `ATLAS_CONNECTOR_TOKEN`, `ATLAS_BASE_URL`, 분리 실행 npm script와 README 운영 안내를 현재 실행 경로에서 제거한다.
- [x] 기준선·OAuth 정본·README·스크린샷 카탈로그·API 상태 설명을 통합 runtime 구조와 일치시킨다.
- [x] 이전 계획은 개발 이력으로 보존하고 이 문서에 실행 결과를 기록한다.
- [x] 기존 `connector/` 디렉터리와 `.connector-dist/` 빌드 산출물을 물리 삭제한다. 2026-08-08 명시 승인 후 삭제했고, 통합 `server/runtime/`만 실행 경로로 남겼다.

### 자체 테스트

- [x] `npm test` 178/178, `npm run lint`, 전체 TypeScript 검사, production build를 통과한다.
- [x] 현재 실행 소스·환경 예시·README·현재 OAuth 문서에서 분리 실행 요구와 API Key 요구가 남지 않았는지 감사한다. 이전 계획의 용어는 개발 이력으로 보존한다.
- [x] `npm run db:baseline`으로 853문서·89,669노드·94,488관계, orphan 0·중복 0·stage 0·integrity `ok`·고정 fingerprint를 확인한다.
- [x] 실제 브라우저에서 임시 Markdown 2건을 추가해 `+7N/+6E`, `+3N/+2E` 영수증, 열린 corpus의 revision polling 자동 갱신, 전체 검색, document 1·2-hop 궤도, 삭제 후 기준선 복구를 확인했다.
- [x] Gold Graph와 최대 밀도 화면을 통합 runtime 정리 후 실제 브라우저로 재확인한다.

### 이슈 및 수정

- [x] 재사용 실행 코드가 기존 폴더에 남아 있어 실행 경로를 혼동할 수 있던 문제를 `server/runtime/` 독립 빌드·script·상태 Route로 분리했다.
- [x] 이전 상태 테이블을 가진 기존 로컬 D1에서 데이터가 유실되지 않도록 migration `0016`이 신규 테이블로 최신 상태만 복사하도록 했다.
- [x] 기존 상태 테이블이 실제로 존재할 때 SQLite `INSERT … SELECT … ON CONFLICT` 구문이 실패해 대시보드가 빈 상태로 보이던 문제를 수정하고, Codex·GitHub 이관 경로 회귀 테스트를 각각 추가했다.
- [x] 기존 폴더의 미커밋 변경은 명시 승인에 따라 제거했다. Git에는 삭제 변경으로 남아 있어, 별도 커밋 전까지 되돌릴 수 있다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 핵심 개발 완료

### 구현·검증 기록 — 2026-08-08 KST

- `server/runtime/client.ts`, `runner.ts`, `run-policy.ts`, `config.ts`가 기존 실행 책임을 가져가며, `npm run dev`와 `npm start`는 이 모듈만 자동 시작한다.
- `app/lib/auth/runtime-access.ts`는 내부 실행기 식별자와 프로세스 내부 secret만 검증한다. 브라우저 요청, 토큰 bearer 인증, 외부 공개 상태 보고는 허용하지 않는다.
- `/api/runtime/status`는 통합 실행기에서만 Codex·GitHub 상태를 기록한다. 이전 공개 heartbeat·capability 경로는 삭제했다.
- `0016_integrated_runtime_status.sql`과 repository 초기화 전환은 상태 테이블만 추가·마이그레이션하며, 그래프 문서·노드·관계 테이블을 수정하지 않는다.
- `npm test` 178/178, `npm run lint`, `npx tsc --noEmit`, `npm run db:baseline`, `npm run graph:audit`, `git diff --check`를 통과했다. D1 fingerprint는 `fb0456967b27077f6bb52a30d941c5dd97883d014d0748d4fdb2d3629ed9675d`로 유지됐다.
- 브라우저에서 overview `130N/176E`와 전체 D1 `89,669N/94,488E`, Gold `68N/101E`, 최대 밀도 `500N/2,000선`, 대시보드 `853문서/89,669노드/94,488관계/115저장소`를 확인했다.
- 실제 브라우저 E2E는 `atlas-phase7-e2e-knowledge.md`에서 업로드 `+7N/+6E` → 전체 검색 `PhaseSevenProbe` → 문서 중심 `7N/6E`, CORE·직접 1-hop·확장 2-hop, 저장 관계 6개·비저장선 0개 → 삭제를 확인했다. 별도의 `atlas-phase7-live-refresh.md`는 열린 corpus 화면에서 `89,669N/94,488E → 89,672N/94,490E → 89,669N/94,488E` 자동 갱신·삭제 복귀를 확인했다.
- 사용자 명시 승인 후 구형 `connector/`와 `.connector-dist/`를 물리 삭제했다. root `tsconfig.json`에서도 구형 폴더 제외 규칙을 제거했다.
- 삭제 후 전체 검증을 재실행해 `npx tsc --noEmit`, `npm run lint`, `npm test` 178/178, production build, runtime build, `npm run db:baseline`, `npm run graph:audit`, `git diff --check`를 모두 통과했다. D1 fingerprint는 변경 없이 `fb0456967b27077f6bb52a30d941c5dd97883d014d0748d4fdb2d3629ed9675d`다.
- 구형 용어 감사 결과 현재 실행 소스·환경 예시·npm scripts·README·현재 OAuth 문서에는 `connector:start`, `ATLAS_CONNECTOR_TOKEN`, `ATLAS_BASE_URL`, `NO SIGNAL`, `Connector 오프라인`이 없다. 남은 문자열은 과거 개발 계획, Gold fixture 근거, 또는 이전 상태 테이블에서 신규 상태 테이블로 복사하는 migration 호환 코드다. `OPENAI_API_KEY`·`LIGHTRAG_API_KEY`는 사용 요구가 아니라 SDK 자식 환경에서 제거하는 보안 목록과 그 회귀 테스트, README의 미사용 선언에만 남는다.

## Phase 8. Codex 선별 보강 배치 안전장치·소량 실행 검증

### 목표

- 기존 대기열을 무차별 처리하지 않고, OAuth 기반 Codex 관계 보강을 선택·제한·감사 가능한 단위로 실행한다.

### 구현 태스크

- [x] `ATLAS_RUNTIME_ENRICHMENT_ONLY=true`로 GitHub source·그래프 답변 작업을 건너뛰는 보강 전용 실행을 지원한다.
- [x] `ATLAS_RUNTIME_JOB_IDS` 또는 `--job-ids`로 최대 25개의 허용 job ID만 claim하는 경계를 추가한다.
- [x] `ATLAS_RUNTIME_MAX_JOBS`, `ATLAS_RUNTIME_MAX_RUNTIME_MS`와 결합해 소량 실행의 작업 수·시간 상한을 강제한다.
- [x] runtime 상태에 처리·성공·경고·실패·종료 사유를 남기고 README에 안전 실행 명령을 기록한다.

### 자체 테스트

- [x] Memory·D1 저장소가 선택하지 않은 이전 작업을 건드리지 않고 선택한 job ID만 원자 claim한다.
- [x] 환경 변수·CLI parser가 보강 전용, job ID 상한, 빈/잘못된 선택값을 안전하게 처리한다.
- [x] `npm test` 179/179, lint, TypeScript, build, D1·graph audit를 통과한다.

### 실제 소량 실행 기록 — 2026-08-08 KST

- 실행 전 `npm run db:baseline -- --backup --restore-check`로 656MB 로컬 D1 backup과 SHA-256 영수증을 만들고 복구 DB의 integrity·fingerprint 일치를 확인했다.
- 구형 provider 대기열은 통합 runtime이 의도적으로 claim하지 않는다. `coreline-ai/memory_node_graph` README·개발 계획과 `coreline-ai/coreline-kakaobot-v3` README를 명시 재인덱싱해 현재 provider 작업 78개를 생성하고, 대응하는 이전 78개 작업은 `stale`로 보존했다.
- 현재 provider의 서로 다른 문서 작업 3개만 선택해 `enrichment_only=true`, `max_jobs=3`, 15분 상한으로 실행했다. 모두 `completed`, 경고·실패 0, 종료 사유 `job_limit`이었다.
- 세 결과는 근거 있는 entity mention 12개를 반환했지만 새 관계는 0개였다. 이는 억지 관계를 저장하지 않은 정상 결과이며 기존 저장 관계 94,488개·기존 Codex 관계 1개를 변경하지 않았다. 따라서 대기열을 확대 실행하지 않고, 다음 배치는 관계가 명시된 청크를 별도 품질 검토 후 선택하는 정책으로 유지한다.
- document scope API에서 세 대상 문서를 각각 `261N/337E`, `255N/343E`, `351N/336E`로 조회했고, corpus·overview·repository·Gold·500×2,000 audit도 모두 통과했다.

## 잔여 리스크 / 후속 과제

- Cloudflare 원격 D1과 외부 공유 웹 배포는 핵심 로컬 개발 완료 후 별도 계획으로 진행한다.
- 서버형 GitHub OAuth와 다중 사용자 세션은 배포 대상과 권한 모델이 확정된 뒤 별도 보안 검토가 필요하다.
- 현재 10,900개 대기 작업(구형 provider 10,825개, 현재 provider 75개)은 자동 일괄 실행하지 않는다. 다음 배치는 관계가 명시된 청크의 품질 검토 후 별도 승인으로 진행한다.

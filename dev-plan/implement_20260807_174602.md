# implement_20260807_174602.md

작성 일시: `2026-08-07 17:46:02 KST`

> **완료 이력 / 후속 전환:** 이 문서까지의 D1 cache·Graph RAG·증분 동기화·분리형 OAuth Connector 구현은 완료 이력이다. 별도 Connector와 `NO SIGNAL`을 제거하는 후속 정본은 [현재 OAuth 런타임 방향](../docs/current-oauth-runtime.md)과 [implement_20260807_203534.md](./implement_20260807_203534.md)다.

이 문서는 실제 D1 지식 그래프 구현 이후 남은 운영·성능·RAG·동기화 과제를 단계적으로 진행하되, 장시간 Codex 실행이나 배포를 암묵적으로 시작하지 않도록 범위를 고정하는 후속 작업 문서다.

## 개발 목적

- 완료된 그래프 계획과 과거 계획의 상태를 구분해 남은 작업을 정확히 관리한다.
- 전체 코퍼스의 반복 조회 비용을 줄이면서 데이터 변경 시 오래된 snapshot이 남지 않게 한다.
- 10,903개 Codex 대기 작업을 제한된 배치로 안전하게 처리할 운영 경계를 마련한다.
- 기존 D1·OAuth Connector를 재사용하는 근거 기반 Graph RAG 질의 흐름을 추가한다.
- 수동 GitHub 동기화가 안정된 뒤 증분 동기화와 최종 Git 전달 상태를 정리한다.

## 개발 범위

- 과거 계획의 superseded 상태 표기와 현재 정본 연결
- D1 corpus snapshot fingerprint·메모리 cache·변경 감지
- Codex 대기열 제한 실행·중지·진행률 계약
- 키워드·그래프 이웃·근거를 사용하는 Graph RAG retrieval API
- OAuth Connector를 통한 선택형 답변 생성과 근거 반환
- GitHub 증분 동기화·운영 로그·수동 fallback
- 최종 테스트와 Git 변경사항 전달 준비

## 제외 범위

- 이번 첫 증분에서 10,903개 Codex 작업 자동 실행
- OpenAI API 키 또는 LightRAG 서버 도입
- 근거 없는 관계·답변 생성
- 전체 89,669노드의 브라우저 동시 렌더링
- 사용자 확인 없는 공개 배포
- 운영 D1 마이그레이션 자동 적용

## 참조 문서

- [프로젝트 README](../README.md)
- [전체 관계형 지식 그래프 개발 계획](./implement_20260805_201928.md)
- [실제 D1 그래프 복구·전체 투영 계획](./implement_20260807_063556.md)
- [과거 GitHub 수집 계획](./implement_20260804_160905.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase 자체 테스트 완료 후 시작한다.
- 10,903개 대기 작업은 구현 검증과 실제 장시간 실행을 분리한다.
- OAuth 로그인 정보와 GitHub 토큰은 브라우저·D1·로그에 저장하지 않는다.
- cache는 성능 최적화일 뿐 정본이 아니며 D1 변경 fingerprint가 달라지면 즉시 폐기한다.
- RAG 응답은 node·relation·source block 근거를 반환하고 근거가 없으면 답변을 보류한다.
- 체크박스는 실제 코드·테스트·브라우저 상태와 맞게 업데이트한다.
- 기존 의존성과 API를 우선 사용하며 불필요한 별도 서버를 추가하지 않는다.

## Phase 상태 요약

- [x] Phase 1. 계획 정합성·corpus cache
- [x] Phase 2. Codex 대기열 제한 실행
- [x] Phase 3. Graph RAG retrieval
- [x] Phase 4. OAuth Connector 답변 생성
- [x] Phase 5. GitHub 증분 동기화
- [x] Phase 6. 최종 전달·Git 정리

## QA 관점

- [x] 같은 fingerprint의 반복 corpus 요청은 같은 결과를 반환하고 무거운 후보 조회를 재사용한다.
- [x] 문서·엔티티·관계가 바뀌면 cache가 즉시 무효화된다.
- [x] cache 객체가 후속 분석의 metrics 변경으로 오염되지 않는다.
- [x] Connector 중단·오프라인·작업 실패에서 제한 배치가 재개 가능하다.
- [x] RAG는 입력 injection을 명령으로 실행하지 않고 근거 검색어로만 취급한다.
- [x] RAG 검색 context의 모든 인용이 현재 D1 node·relation·source와 일치한다.
- [x] GitHub 증분 동기화 실패가 이전 정상 그래프를 훼손하지 않는다.

## Phase 1. 계획 정합성·corpus cache

### 목표

- 현재 정본 계획을 명확히 하고 변경 감지가 가능한 corpus cache로 반복 로딩 비용을 줄인다.

### 구현 태스크

- [x] 과거 계획 문서 상단에 역사 기록이며 최신 계획으로 대체됐음을 명시한다.
- [x] 문서 최신 갱신 시각과 문서·엔티티·관계 수·row version으로 corpus fingerprint를 계산한다.
- [x] 같은 fingerprint에서는 제한 조회한 corpus 후보 snapshot을 재사용한다.
- [x] 반환 snapshot을 복제해 consolidation·analytics가 cache 원본을 변경하지 못하게 한다.
- [x] 변경 fingerprint·빈 메모리 저장소·overview·repository scope에는 기존 동작을 유지한다.

### 자체 테스트

- [x] cache hit·miss·fingerprint 변경을 결정적으로 확인한다.
- [x] cache 반환값 변경이 다음 요청으로 누출되지 않는다.
- [x] corpus 500노드·최대 2,000관계와 D1 전체 수량이 유지된다.
- [x] lint·TypeScript·관련 테스트·브라우저 console을 확인한다.

### 이슈 및 수정

- [x] cache된 후보를 그대로 반환하면 analytics metrics나 태그 배열 변경이 다음 요청에 누출될 수 있어 저장 시와 반환 시 모두 `structuredClone`으로 격리했다.
- [x] 브라우저 반복 화면 측정에서 동일 500노드·1,539관계를 유지하며 첫 확인 2,168ms, cache 재사용 확인 1,637ms, console error 0건을 기록했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 2 진행 가능

## Phase 2. Codex 대기열 제한 실행

### 목표

- 10,903개 대기 작업을 무제한 자동 실행하지 않고 사용자가 지정한 작은 배치로 실행·중지·재개한다.

### 구현 태스크

- [x] Connector에 `max jobs`·`max runtime` 실행 상한을 추가한다.
- [x] 처리 전 예상 작업 수와 완료 후 성공·경고·실패·잔여 수량을 영수증으로 기록한다.
- [x] 대시보드에 소규모 실행 안내와 중지·재개 상태를 표시한다.
- [x] 최초 기본값은 dry-run 또는 1개 작업으로 제한한다.

### 자체 테스트

- [x] 0·1·N개 상한과 중간 종료를 확인한다.
- [x] Lease·idempotency·수동 재시도 계약이 유지된다.
- [x] 실제 장시간 실행 없이 fixture로 배치 완료를 검증한다.

### 이슈 및 수정

- [x] 최대 실행 시간과 작업 수 상한이 동시에 충족될 때 `job_limit`이 시간 종료를 덮어쓰는 문제를 수정해 `runtime_limit`을 우선 보존했다.
- [x] npm 뒤에 전달한 `--max-jobs=N`이 기본값보다 우선하도록 `--batch`가 1개·5분 기본값만 제공하는 구조로 분리했다.

### 증분 실행 기록 — Phase 2 제한 배치 (2026-08-07 KST)

- `connector/run-policy.ts`에 0~100개 작업 상한, 최대 24시간 실행 상한, enrichment-only·빈 대기열 종료 계약과 구조화 영수증을 추가했다.
- `connector:dry-run`은 작업을 Claim하지 않고 대기열만 확인하며, `connector:batch`는 기본 1개·최대 5분으로 실행한다. 추가 인수로 검토된 N개 상한을 명시할 수 있다.
- 실행 시작·주기 신호·종료 시 처리·성공·주의·실패와 종료 이유를 heartbeat에 저장하고 대시보드 `SAFE RUN POLICY`에서 최근 상태와 안전 중지·재개 명령을 표시한다.
- 실제 로컬 dry-run은 대기 **10,903개**, 처리 0개, 잔여 **10,903개**, `dry_run`으로 완료됐다. Codex 작업 Claim·LLM 호출·관계 변경은 없었다.
- 자동 회귀는 dry-run 0개, 기본 1개, N개 순차 처리, 실행 시간 중단·재시도 가능 실패, 메모리·D1 heartbeat 저장을 확인했다.
- 검증: `npm test` 전체 135개, `npm run lint`, `npx tsc --noEmit`, 프로덕션·Connector build, 실제 대시보드 `DRY RUN · 0/0 · 5분 · 대기 10,903개` 표시를 통과했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 3 진행 가능

## Phase 3. Graph RAG retrieval

### 목표

- 질문에서 핵심 키워드를 정규화하고 관련 node·1/2-hop relation·source evidence를 제한 조회한다.

### 구현 태스크

- [x] 질문 검증·길이 제한·정규화 계약을 추가한다.
- [x] D1 label·summary·tag·source block 검색과 그래프 이웃 확장을 구현한다.
- [x] 중심성·관계 confidence·근거 완전성으로 context를 정렬한다.
- [x] `/api/graph/query`가 답변 전 retrieval context와 인용 후보를 반환하게 한다.

### 자체 테스트

- [x] 일치 없음·단일 허브·다중 커뮤니티·긴 질문을 확인한다.
- [x] 결과가 node·edge 예산과 source 인용 상한을 지킨다.
- [x] SQL wildcard·HTML·prompt injection 입력을 안전하게 처리한다.

### 이슈 및 수정

- [x] FTS block 결과를 mention과 먼저 조인하면 공통 검색어에서 중간 row가 폭증하는 문제를 수정했다. FTS 상위 block을 CTE에서 먼저 48개로 제한한 뒤 문서·mention 근거를 결합한다.
- [x] `구조·시스템·구현·기능·개발·프로젝트·문서` 같은 일반어가 고차수 Phase 노드를 과도하게 올리는 문제를 줄이도록 한국어 검색 불용어와 키워드 적합도 우선 가중치를 적용했다.

### 증분 실행 기록 — Phase 3 Graph RAG retrieval (2026-08-07 KST)

- `graph-retrieval.ts`에 500자 질문 검증, NFKC·제어문자·공백 정규화, 최대 6개 핵심어, 노드 48·관계 96·인용 24 상한을 추가했다.
- D1 FTS5는 엔티티 label·summary·tag와 148,655개 문서 block을 검색하고 insert·update·delete trigger로 현재 그래프와 동기화한다. FTS5를 지원하지 않는 축소 SQLite 환경에서는 escape된 bound `LIKE` 검색으로 fallback한다.
- 검색 seed에서 confidence를 반영해 1-hop·2-hop을 확장하고 lexical relevance·부분 그래프 중심성·confidence·근거 완전성을 결합해 노드·관계를 결정적으로 정렬한다.
- GET·POST `/api/graph/query`는 LLM 답변이 아닌 retrieval context, commit/line source URL, `answerReady`와 검색 예산을 반환한다.
- 실제 D1에서 `에이전트 메모리 검색 구조`를 검색해 정규화 검색어 3개, 노드 8개·관계 1개·인용 6개와 `answerReady=true`를 확인했다. 초기 FTS backfill은 엔티티 89,669개·block 148,655개를 일치시켰고, 같은 로컬 개발 환경의 후속 조회는 약 2.23초였다.
- 검증: `npm test` 전체 142개, `npm run lint`, `npx tsc --noEmit`, 프로덕션·Connector build, `git diff --check`, 실제 로컬 GET·POST query smoke를 통과했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 4 진행 가능

## Phase 4. OAuth Connector 답변 생성

### 목표

- Graph RAG context를 기존 Codex OAuth Connector에 전달하고 근거가 있는 구조화 답변을 반환한다.

### 구현 태스크

- [x] 질의 작업 계약·Lease·취소·재시도 상태를 기존 Connector 패턴으로 추가한다.
- [x] Codex SDK 구조화 출력에 answer·citation IDs·uncertainty를 요구한다.
- [x] 서버에서 citation과 retrieval context를 재검증한 뒤 결과를 노출한다.
- [x] Connector 오프라인이면 retrieval 결과만 제공하고 답변 생성 불가를 명확히 표시한다.

### 자체 테스트

- [x] API 키 없이 기존 `codex login` 경계를 사용한다.
- [x] 존재하지 않는 citation·근거 밖 단정을 거부한다.
- [x] cancel·timeout·Connector 오프라인 복구를 확인한다.

### 이슈 및 수정

- [x] Connector가 오프라인인데 답변 작업을 먼저 쌓으면 처리 시점을 알 수 없는 대기열이 생기므로, 45초 이내 온라인 heartbeat가 있을 때만 작업을 등록하고 그 외에는 retrieval context와 `connector_offline`을 반환하도록 수정했다.
- [x] answer 자유 텍스트에 인용 없는 문장을 덧붙일 수 있는 여지를 없애기 위해, 모든 claim에 현재 context citation을 요구하고 answer가 claim 텍스트의 순서 결합과 정확히 일치하는지 서버에서 재검증한다.

### 증분 실행 기록 — Phase 4 OAuth Connector 답변 생성 (2026-08-07 KST)

- `graph-answer-contracts.ts`에 질문·retrieval context fingerprint 기반 멱등 작업, 구조화 answer·claims·citation IDs·uncertainty·limitations 계약을 추가했다.
- Memory·D1 `graph_answer_jobs` 저장소와 `0013_graph_answer_jobs.sql`에 Claim·Lease·running·complete·fail·cancel·최대 2회 수동 retry 상태를 추가했다.
- POST `/api/graph/query`의 `generateAnswer: true`는 근거 부족 시 `insufficient_evidence`, Connector 오프라인 시 `connector_offline`과 검색 context만 반환하고, 온라인일 때만 답변 작업을 등록한다.
- `/api/graph/query-jobs` 작업 API와 Connector client/runner를 연결했다. 기본 Connector 우선순위는 GitHub source → 사용자 Graph 답변 → 10,903개 일반 관계 보강이며, `--enrichment-only` 제한 배치는 기존처럼 답변 작업을 건드리지 않는다.
- 공식 `@openai/codex-sdk`는 read-only sandbox·network disabled·approval never 구조화 출력으로 실행하고, context 내부 문자열을 명령이 아닌 불신 데이터로 취급한다. 서버는 결과 제출 시 모든 claim citation을 고정 context와 다시 대조한다.
- 키 환경 변수를 제거한 `codex login status`에서 `Logged in using ChatGPT`를 확인했다. 자동 테스트는 실제 모델 호출 없이 구조화 engine 경계와 API를 fixture로 검증해 기존 대기열을 Claim하지 않았다.
- 검증: `npm test` 전체 147개, 프로덕션·Connector build, Memory·D1 Lease 동등성, OAuth 쓰기 경계, invalid citation·claims 밖 답변 거부, cancel·timeout·offline→online 재요청을 통과했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 5 진행 가능

## Phase 5. GitHub 증분 동기화

### 목표

- 수동 동기화 계약을 유지하면서 변경 저장소만 안전하게 preview·apply한다.

### 구현 태스크

- [x] 마지막 성공 commit·manifest digest 기반 변경 감지를 추가한다.
- [x] 수동 실행을 기본으로 두고 예약/Webhook 진입점은 동일 작업 API를 사용한다.
- [x] rate limit·권한 변경·삭제·부분 실패를 저장소별로 격리한다.
- [x] 성공 후 corpus fingerprint가 바뀌어 cache가 무효화되는지 확인한다.

### 자체 테스트

- [x] no-op·create·update·delete·권한 상실을 확인한다.
- [x] 이전 정상 그래프 보존과 재시도 영수증을 검증한다.

### 이슈 및 수정

- [x] 여러 저장소를 한 Preview에 묶으면 한 저장소의 권한·rate limit 실패가 전체 batch를 실패시키므로 증분 API는 저장소마다 Preview 작업 1개를 등록하도록 수정했다.
- [x] 예약·Webhook이 Apply까지 수행하면 기존 사용자 승인 경계가 사라지므로 공통 API에서 두 trigger는 Preview만 허용하고, 같은 run의 완료 Preview ID를 명시한 수동 요청만 Apply하도록 제한했다.
- [x] commit만 비교하면 삭제·로컬 상태 불일치·과거 digest 누락을 놓칠 수 있어 마지막 성공 commit·단일 저장소 manifest digest·현재 Blob dry-run을 함께 비교한다.

### 증분 실행 기록 — Phase 5 GitHub 증분 동기화 (2026-08-07 KST)

- `incremental-sync.ts`는 마지막 성공 Apply 영수증의 `commitSha`·`manifestDigest`와 현재 문서 Blob을 완료된 단일 저장소 Preview에 대조해 `changed`·`unchanged`·`blocked`·실패 상태를 결정한다.
- `GET`·`POST /api/github/incremental-sync`는 `manual`·`schedule`·`webhook`이 공유하는 run API다. 모든 Preview를 저장소별 작업으로 격리하고 요청마다 생성·재사용·변경 없음·차단·다른 run 활성·거부 영수증을 반환한다.
- Apply는 같은 `runId`의 저장소별 완료 Preview 작업 ID를 사용자가 모두 명시한 수동 요청만 허용한다. 변경 없는 저장소는 Apply 작업을 만들지 않으며 변경 저장소만 기존 원자 Apply·stage·Blob 재사용 경로에 등록한다.
- 권한 상실·rate limit·원격 오류는 해당 저장소 작업에만 기록되고 마지막 성공 commit·digest·문서·노드·관계 집계는 유지된다. 실패 작업의 기존 최대 2회 수동 재시도 계약도 보존된다.
- D1 회귀에서 같은 fingerprint의 반복 corpus 조회 뒤 새 저장소 2개를 원자 적용해 문서·엔티티 수가 즉시 바뀐 새 snapshot을 반환함을 확인했다.
- 검증: `npm run lint`, `npx tsc --noEmit`, 프로덕션·Connector build, `git diff --check`, 전체 **151개 테스트**를 통과했다. 실제 원격 GitHub Preview·Apply나 배포는 자동 실행하지 않았다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] Phase 6 진행 가능

## Phase 6. 최종 전달·Git 정리

### 목표

- 모든 계획·코드·테스트 상태를 맞추고 검토 가능한 Git 변경 단위로 전달한다.

### 구현 태스크

- [x] README·개발 계획·실제 D1 수량을 최종 동기화한다.
- [x] 민감정보·대용량 임시 파일·로컬 D1이 Git 대상에 포함되지 않는지 확인한다.
- [x] 변경 파일을 기능 단위로 검토하고 사용자가 요청한 브랜치에 commit·push한다.

### 자체 테스트

- [x] lint·TypeScript·build·전체 테스트·diff check를 통과한다.
- [x] 로컬 corpus·overview·repository·Gold·max 화면을 확인한다.

### 이슈 및 수정

- [x] 공개 GitHub 저장소에 전체 수집·분석 영수증을 포함하면 비공개 저장소 이름·commit·문서 근거가 노출될 수 있어 운영 산출물을 `.gitignore`에 추가했다.
- [x] 로컬 D1 object ID를 스크립트에 하드코딩하면 다른 기기에서 감사·재처리 명령이 실패하므로 단일 로컬 D1 자동 탐색과 다중 DB 명시 선택 계약으로 교체했다.
- [x] 대시보드 99,393 노드와 corpus 89,669 노드가 다른 이유를 문서별 노드 합계와 canonical 고유 엔티티 수의 차이로 README에 명시했다.

### 증분 실행 기록 — Phase 6 전달 준비 (2026-08-07 KST)

- 실제 로컬 D1을 다시 감사해 문서 853개·문서별 노드 합계 99,393개·고유 엔티티 89,669개·규칙 관계 94,487개·Codex 관계 1개·대기 보강 10,903개를 확인했다.
- `graph:audit` 범위를 corpus·overview·대표 repository·Gold Graph·최대 밀도 fixture로 확장했다. 각각 500/1,539, 130/176, 500/276, 68/101, 500/2,000 노드/관계를 반환했다.
- `graph:reprocess` preview는 853문서·148,655블록·10,904청크를 확인했으며 `--execute`를 사용하지 않아 D1을 재처리하지 않았다. `github:audit-all`은 111개 저장 저장소·853문서·무결성 이슈 0건으로 PASS했다.
- 공개 대상 `coreline-ai/memory_node_graph`가 PUBLIC임을 확인한 뒤 전체 GitHub 수집·no-op·corpus 분석 영수증을 공개 Git 대상에서 제외했다. `.wrangler`, D1·backup, Connector build, `dist`, 환경 파일도 기존 ignore 규칙으로 제외된다.
- 자격 증명 유사 문자열은 보안 거부를 검증하는 명시적 테스트 fixture 네 곳뿐이며 실제 GitHub/OpenAI token·private key는 전달 후보에서 발견되지 않았다.
- 검증: `npm run lint`, `npx tsc --noEmit`, 프로덕션·Connector build, `git diff --check`, 전체 **152개 테스트**를 통과했다.
- 통합 구현 커밋 `5b0dd00`을 공개 저장소 `coreline-ai/memory_node_graph`의 `main` 브랜치에 push했다.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 후속 개발 계획 종료
